export class FreezeResult {
    frozen;
    reference;
    note;
    constructor(frozen, reference, note) {
        this.frozen = frozen;
        this.reference = reference;
        this.note = note;
    }
}
//# sourceMappingURL=freeze-result.js.map